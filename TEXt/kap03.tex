%%% Fiktivní kapitola s ukázkami tabulek, obrázkù a kódu

\chapter{Tabulky, obrázky, programy}

Používání tabulek a grafù v~odborném textu má nìkterá spoleèná
pravidla a~nìkterá specifická. Tabulky a grafy neuvádíme pøímo do
textu, ale umístíme je buï na samostatné stránky nebo na vyhrazené
místo v~horní nebo dolní èásti bìžných stránek. \LaTeX\ se o~umístìní
plovoucích grafù a tabulek postará automaticky.

Každý graf a tabulku
oèíslujeme a umístíme pod nì legendu. Legenda má popisovat obsah grafu
èi tabulky tak podrobnì, aby jim ètenáø rozumìl bez dùkladného
studování textu práce.

Na každou tabulku a graf musí být v~textu odkaz
pomocí jejich èísla. Na pøíslušném místì textu pak shrneme ty
nejdùležitìjší závìry, které lze z~tabulky èi grafu uèinit. Text by
mìl být èitelný a srozumitelný i~bez prohlížení tabulek a grafù a
tabulky a grafy by mìly být srozumitelné i~bez podrobné èetby textu.

Na tabulky a grafy odkazujeme pokud možno nepøímo v~prùbìhu bìžného
toku textu; místo \emph{\uv{Tabulka~\ref{tab03:Nejaka} ukazuje, že
    muži jsou v~prùmìru o~$9,9\,\rm kg$ tìžší než ženy}} radìji napíšeme
\emph{\uv{Muži jsou o~$9,9\,\rm kg$ tìžší než ženy (viz
    Tabulka~\ref{tab03:Nejaka})}}.

\section{Tabulky}

\begin{table}[b!]

\centering
%%% Tabulka používá následující balíèky:
%%%   - booktabs (\toprule, \midrule, \bottomrule)
%%%   - dcolumn (typ sloupce D: vycentrovaná èísla zarovnaná na
%%%     desetinnou èárku
%%%     Všimnìte si, že ve zdrojovém kódu jsou desetinné teèky, ale
%%%     tisknou se èárky.
%%% Dále používáme pøíkazy \pulrad a \mc definované v makra.tex

\begin{tabular}{l@{\hspace{1.5cm}}D{.}{,}{3.2}D{.}{,}{1.2}D{.}{,}{2.3}}
\toprule
 & \mc{} & \mc{\textbf{Smìrod.}} & \mc{} \\
\pulrad{\textbf{Efekt}} & \mc{\pulrad{\textbf{Odhad}}} & \mc{\textbf{chyba}$^a$} &
\mc{\pulrad{\textbf{P-hodnota}}} \\
\midrule
Abs. èlen     & -10.01 & 1.01 & \mc{---} \\
Pohlaví (muž) & 9.89   & 5.98 & 0.098 \\
Výška (cm)    & 0.78   & 0.12 & <0.001 \\
\bottomrule
\multicolumn{4}{l}{\footnotesize \textit{Pozn:}
$^a$ Smìrodatná chyba odhadu metodou Monte Carlo.}
\end{tabular}

\caption{Maximálnì vìrohodné odhady v~modelu M.}\label{tab03:Nejaka}

\end{table}

U~\textbf{tabulek} se doporuèuje dodržovat následující pravidla:

\begin{itemize} %% nebo compactitem z balíku paralist
\item Vyhýbat se svislým linkám. Silnìjšími vodorovnými linkami
  oddìlit tabulku od okolního textu vèetnì legendy, slabšími
  vodorovnými linkami oddìlovat záhlaví sloupcù od tìla tabulky a
  jednotlivé èásti tabulky mezi sebou. V~\LaTeX u tuto podobu tabulek
  implementuje balík \texttt{booktabs}. Chceme-li výraznìji oddìlit
  nìkteré sloupce od jiných, vložíme mezi nì vìtší mezeru.
\item Nemìnit typ, formát a význam obsahu políèek v~tomtéž sloupci
  (není dobré do téhož sloupce zapisovat tu prùmìr, onde procenta).
\item Neopakovat tentýž obsah políèek mnohokrát za sebou. Máme-li
  sloupec \textit{Rozptyl}, který v~prvních deseti øádcích obsahuje
  hodnotu $0,5$ a v~druhých deseti øádcích hodnotu $1,5$, pak tento
  sloupec radìji zrušíme a vyøešíme to jinak. Napøíklad mùžeme tabulku
  rozdìlit na dvì nebo do ní vložit popisné øádky, které informují
o~nìjaké promìnné hodnotì opakující se v~následujícím oddíle tabulky
  (napø. \emph{\uv{Rozptyl${}=0,5$}} a níže \emph{\uv{Rozptyl${}=
      1,5$}}).
\item Èísla v~tabulce zarovnávat na desetinnou èárku.
\item V~tabulce je nìkdy potøebné používat zkratky, které se jinde
nevyskytují. Tyto zkratky mùžeme vysvìtlit v~legendì nebo
v~poznámkách pod tabulkou. Poznámky pod tabulkou mùžeme využít i
k~podrobnìjšímu vysvìtlení významu  nìkterých sloupcù nebo hodnot.
\end{itemize}

\section{Obrázky}

Nìkolik rad týkajících se obrázkù a grafù.

\begin{itemize}
\item Graf by mìl být vytvoøen ve velikosti, v~níž bude použit
  v~práci. Zmenšení pøíliš velkého grafu vede ke špatné èitelnosti
  popiskù.
\item Osy grafu musí být øádnì popsány ve stejném jazyce, v~jakém je
  psána práce (absenci diakritiky lze tolerovat). Kreslíme-li graf
  hmotnosti proti výšce, nenecháme na nich popisky \texttt{ht} a
  \texttt{wt}, ale osy popíšeme \emph{Výška [cm]} a~\emph{Hmotnost
    [kg]}. Kreslíme-li graf funkce $h(x)$, popíšeme osy $x$ a $h(x)$.
  Každá osa musí mít jasnì urèenou škálu.
\item Chceme-li na dvourozmìrném grafu vyznaèit velké množství bodù,
  dáme pozor, aby se neslily do jednolité èerné tmy. Je-li bodù mnoho,
  zmenšíme velikost symbolu, kterým je vykreslujeme, anebo vybereme
  jen malou èást bodù, kterou do grafu zaneseme. Grafy, které obsahují
  tisíce bodù, dìlají problémy hlavnì v~elektronických dokumentech,
  protože výraznì zvìtšují velikost souborù.
\item Budeme-li práci tisknout èernobíle, vyhneme se používání barev.
  Èáry roz\-li\-šu\-je\-me typem (plná, teèkovaná, èerchovaná,\ldots), plochy
  dostateènì roz\-díl\-ný\-mi intensitami šedé nebo šrafováním. Význam
  jednotlivých typù èar a~ploch vysvìtlíme buï v~textové legendì ke
  grafu anebo v~grafické legendì, která je pøímo souèástí obrázku.
\item Vyhýbejte se bitmapovým obrázkùm o~nízkém rozlišení a zejména
  JPEGùm (zuby a kompresní artefakty nevypadají na papíøe pìknì).
  Lepší je vytváøet obrázky vektorovì a vložit do textu jako PDF.
\end{itemize}

\section{Programy}

Algoritmy, výpisy programù a popis interakce s~programy je vhodné
odlišit od ostatního textu. Jednou z~možností je použití {\LaTeX}o\-vé\-ho balíèku
\texttt{fancyvrb} (fancy verbatim), pomocí nìhož je v~souboru \texttt{makra.tex}
nadefinováno prostøedí \texttt{code}. Pomocí nìho lze vytvoøit
napø. následující ukázky.

\begin{code}
> mean(x)
[1] 158.90
> objekt$prumer
[1] 158.90
\end{code}
%$
Menší písmo:
\begin{code}[fontsize=\footnotesize]
> mean(x)
[1] 158.90
> objekt$prumer
[1] 158.90
\end{code}
%$
Bez rámeèku:
\begin{code}[frame=none]
> mean(x)
[1] 158.90
> objekt$prumer
[1] 158.90
\end{code}
%$
Užší rámeèek:
\begin{code}[xrightmargin=20em]
> mean(x)
[1] 158.90
> objekt$prumer
[1] 158.90
\end{code}
%$
