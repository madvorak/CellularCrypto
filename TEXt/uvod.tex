\chapter*{Úvod}
\addcontentsline{toc}{chapter}{Úvod}

Toto je implementaènì--experimentální práce. Cílem práce je vytvoøit šifrovací algoritmus využívající celulární automaty. Úkolem je naprogramovat rùzné zpùsoby natahování šifrovacích klíèù a namìøit zmìøit jejich vlastnosti, za úèelem zvolení správného algoritmu pro šifrovací aplikaci.

Uchovávání dokumentù v tajnosti je velmi dùležité. Kompromitace údajù, které nesmí být veøejné, mùže vést k finanèním ztrátám, nepøíjemným událostem v životì nebo postihùm za porušování zákonù. V souèasné dobì je známo velké množství šifrovacích algoritmù, které slouží právì k tomu, aby nìjaký dokument èi zprávu byl schopen èíst jen jeho vlastník, pøípadnì èlovìk, pro kterého je zpráva urèena.

Není ale zatím moc známo o použití celulárních automatù pro šifrování dat. Ukazuje se, že celulární automaty (napøíklad elementární celulární automat èíslo 30) dokáží generovat data, která jsou i podle pøísných hledisek pseudonáhodná. To nás vede k nadìji, že by z celulárních automatù šel udìlat napøíkald nástroj na protahování šifrovacích klíèù. Celulární automaty jsou velmi univerzální model výpoètù a kdyby se je podaøilo aplikovat na šifrování dat, mohlo by to vést k mnohým výhodám díky jejich jednoduchosti a paralelizovatelnosti. Domníváme se, že takový algoritmus založený na celulárních automatech by mohl být v budoucnu implementován na velmi rozmanitých podobách hardwaru.

\section*{Struktura práce}
\addcontentsline{toc}{section}{Struktura práce}

V 1. kapitole jsou pøedstaveny celulární automaty, jak je rozdìlujeme, co umí a jaké chování vykazují. Ve 2. kapitole se nachází úvod do šifrování a trocha teorie o testování kvality šifrovacích algoritmù. Ve 3. kapitole je pøedstaven vytvoøený program v C\#, z~jakých èástí se skládá a pár zajímavých implementaèních detailù. Tìžištìm práce je 4.~kapitola, ve které jsou rozebírány vyzkoumané možnosti natahování klíèù a výsledky na nich namìøené. V 5. kapitole jsou struènì rozebrány další možnosti využití celulárních automatù v kryptografii.